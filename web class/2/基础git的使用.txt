1.git clone (准备克隆的项目名)//用于将网上的文件下载到本地
2.git status//用于检测本地版本库的修改状态
3.git add (被修改文件的文件名)//将指定的被修改文件添加至本地版本库
  git add .//将所有未提交的被修改文件添加至本地版本库
4.git pull//同步到本地的命令
5.git commit //将本地确认修改的文件保存到本地版本库
  git commit -m “”//引号中的文字代表对提交文件的注释
6.git push origin master (需要提交到的账号)//将最后的本地本地版本库发送到github仓库